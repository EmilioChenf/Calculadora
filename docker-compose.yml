services:
  calculadora:
    build: .
    ports:
      - "5173:5173"
      - "6006:6006"
    volumes:
      - .:/app
      - calculadora_node_modules:/app/node_modules
    command: bun run dev -- --host 0.0.0.0

volumes:
  calculadora_node_modules:
